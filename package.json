{
  "name": "json-schemas",
  "private": true,
  "scripts": {
    "test": "bun run build && bun scripts/test.js",
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "glob": "^11.0.3",
    "prettier": "^3.6.2",
    "smol-toml": "^1.4.2",
    "yaml": "^2.8.1"
  }
}
